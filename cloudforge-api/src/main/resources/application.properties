# CloudForge API — optional defaults (overridden by application-local.properties / env)

# Optional CloudForge Manager defaults for local operator tooling
cfc.manager.url=http://127.0.0.1:1958
cfc.manager.target=ministack
