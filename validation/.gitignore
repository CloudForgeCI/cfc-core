# CDK artifacts (contains AWS account numbers and sensitive info)
cdk.out/
cdk.context.json
deployment-context.json

# CDK output and timing files
cdk-output.log
synth_times.txt
cdktf.out/

# AWS credentials and config files
.aws/
aws-credentials.json
aws-config.json
credentials
config

# Environment files with secrets
.env
.env.local
.env.production
.env.staging
.env.development

# Log files that might contain sensitive info
*.log
logs/

# Temporary files
*.tmp
*.temp
*.swp
*.swo
*~

# IDE files
.idea/
.vscode/
*.iml

# OS files
.DS_Store
Thumbs.db

# Backup files
*.bak
*.backup
*.orig

# Validation Results
validation-results

test-results

scripts/validation-results
scripts/synth-results

benchmark-results

# Local MiniStack bind-mount data (Jenkins home, etc.)
.ministack-volumes/
.localstack-volumes/

# Runtime output written by the local panel/deploy tooling and the compliance-matrix/truth-table
# generator scripts when they run, not source fixtures.
panel-stacks.json
deployment-context.jsonBAK
compliance-matrix/
deployment-contexts/*-app-*.json
deployment-contexts/CFCompliance-*.json
!deployment-contexts/CFCompliance-allframeworks-production-ec2-LocalStack.json
deployment-contexts/Jenkins-Ec2-Probe.json
deployment-contexts/compliance/*-jenkins*.json
