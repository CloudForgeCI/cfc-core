<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.cloudforgeci</groupId>
  <artifactId>cfc-core</artifactId>
  <version>3.2.18-SNAPSHOT</version>
  <packaging>pom</packaging>

  <name>CloudForge Core BOM</name>
  <description>
    Bill of Materials (BOM) for CloudForge — import as type pom / scope import.
    Modules: cloudforge-core, cloudforge-api, cloudforge-ministack, cloudforge-localstack.
    Sample entrypoint: cfc-testing (not in BOM). cloudforge-manager-deployment and
    cloudforge-manager are no longer part of this BOM/reactor — split into their own private repos
    (CloudForgeCI/cloudforge-manager-deployment, CloudForgeCI/cloudforge-manager), each with a
    standalone pom.xml and independent versioning.
    See docs/architecture/cloudforge-sample-bom.template.md and
    docs/architecture/module-boundaries-and-refactor.plan.md.
  </description>
  <url>https://github.com/CloudForgeCI/cfc-core</url>

  <licenses>
    <license>
      <name>Apache License, Version 2.0</name>
      <url>https://www.apache.org/licenses/LICENSE-2.0.txt</url>
      <distribution>repo</distribution>
    </license>
  </licenses>

  <developers>
    <developer>
      <id>cloudforgeci</id>
      <name>CloudForge CI Team</name>
      <email>support@cloudforge.com</email>
      <organization>CloudForge CI</organization>
      <organizationUrl>https://www.cloudforgeci.com</organizationUrl>
    </developer>
  </developers>

  <properties>
    <!-- Build Properties -->
    <maven.compiler.source>25</maven.compiler.source>
    <maven.compiler.target>25</maven.compiler.target>
    <java.version>25</java.version>
    <build.version.suffix></build.version.suffix> <!-- Blank by default -->

    <!-- Tests are skipped by default: the full suite (cloudforge-api alone runs ~6000 tests)
         takes 15-20+ minutes, too slow for a routine `mvn install`. Enable them with the "ci"
         profile below (`-Pci`). Any CI step that depends on tests must pass `-Pci` explicitly,
         because a bare `mvn install`/`verify` here runs no tests, inverting the Maven default. -->
    <skipTests>true</skipTests>

    <!-- Latest published cloudforge-manager-deployment SNAPSHOT. Kept current by
         .github/workflows/sync-cloudforge-manager-deployment-version.yml, which polls the
         Central snapshots metadata and opens a PR with the bump. Managing the version in this
         BOM gives every consumer (cloudforge-manager, cfc-testing) a single sync point; a BOM
         entry is only a coordinate + version, so it does not tie that project's release cadence
         to cfc-core's. -->
    <cloudforge-manager-deployment.version>3.2.9-SNAPSHOT</cloudforge-manager-deployment.version>

    <!-- AWS CDK Versions -->
    <!-- Note: v2.197.0+ defaults to nodejs22.x for AwsCustomResource which isn't available in all regions -->
    <cdk.version>2.196.0</cdk.version>
    <constructs.version>10.3.0</constructs.version>
    <cdknag.version>2.37.55</cdknag.version>

    <!-- Core Library Versions -->
    <!-- 2.21.6 or later is required: dependency-check reports CVE-2026-68497, CVE-2026-19032,
         CVE-2026-59889 and CVE-2026-54515 against 2.21.4, and still reports two of them against
         2.21.5 (the advisories' stated fix version), so the next patch release is used. -->
    <jackson.version>2.21.6</jackson.version>
    <!-- jackson-annotations versions independently of core/databind (2.20, 2.21, 2.22, no
         patch suffix), so it can't share ${jackson.version} once that has one. -->
    <jackson-annotations.version>2.21</jackson-annotations.version>
    <jakarta.validation.version>3.0.2</jakarta.validation.version>
    <!-- 4.1.136.Final, not the 4.1.107.Final AWS SDK v2 2.25.0's own BOM pulls in transitively
         (via netty-nio-client): dependency-check's SBOM scan found 57-58 CVEs against that
         pinned version, several CRITICAL (DNS cache-poisoning via unvalidated CNAME/NS-record
         bailiwick, HTTP request smuggling via Transfer-Encoding/chunked-extension parsing, XXE in
         XmlDecoder). Netty 4.1.x patch releases are binary-compatible within the same minor line,
         so overriding just the version here (not the AWS SDK's own BOM) is safe. -->
    <netty.version>4.1.136.Final</netty.version>

    <!-- Test Dependency Versions -->
    <jupiter.version>5.12.2</jupiter.version>
    <hibernate.validator.version>8.0.1.Final</hibernate.validator.version>
    <jakarta.el.version>4.0.2</jakarta.el.version>

    <!-- Plugin Versions -->
    <maven.compiler.plugin.version>3.15.0</maven.compiler.plugin.version>
    <maven.source.plugin.version>3.3.0</maven.source.plugin.version>
    <maven.javadoc.plugin.version>3.7.0</maven.javadoc.plugin.version>
    <!-- See the maven-gpg-plugin entry in the release profile for why 3.2.8 is used. -->
    <maven.gpg.plugin.version>3.2.8</maven.gpg.plugin.version>
    <maven.deploy.plugin.version>3.1.1</maven.deploy.plugin.version>
    <jacoco.plugin.version>0.8.13</jacoco.plugin.version>
    <cyclonedx.plugin.version>2.8.2</cyclonedx.plugin.version>
    <owasp.plugin.version>12.1.0</owasp.plugin.version>
    <central.publishing.plugin.version>0.9.0</central.publishing.plugin.version>
    <flatten.plugin.version>1.7.3</flatten.plugin.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <!--
        CloudForge Modules (BOM)

        cloudforge-core            Shared contracts, DeploymentConfig, com.cloudforge.core.local
        cloudforge-api             CloudForgeDeployment, ApplicationSpecs, CMS, CDK factories
        cloudforge-ministack       MiniStack adapter/deployer/pipeline (depends: core)
        cloudforge-localstack      LocalStack adapter/deployer/pipeline (depends: core)
        cloudforge-manager-deployment   ApplicationSpec/deploy presets for CloudForge Manager
                                        (CloudForgeCI/cloudforge-manager-deployment); released
                                        from its own repo on its own cadence. Listed here as a
                                        managed coordinate + version only, not a reactor module
                                        (see the cloudforge-manager-deployment.version property).

        cloudforge-synth-service and cloudforge-manager are NOT managed here — each lives in its
        own repo (CloudForgeCI/cloudforge-synth-service, CloudForgeCI/cloudforge-manager) with
        independent versioning; a consumer that needs them declares an explicit <version> for
        those coordinates directly. cloudforge-synth-service declares this pom as its Maven
        <parent> (resolved from the repository, not reactor membership), which is how it inherits
        everything below without being listed here. cloudforge-manager imports this BOM like any
        other consumer.

        Consumer sample: docs/architecture/cloudforge-sample-bom.template.md
        Not in BOM: cfc-testing (monorepo entrypoint / cloudforge-sample reference); it imports
        this BOM like any consumer but is not itself a managed coordinate.
      -->
      <dependency>
        <groupId>com.cloudforgeci</groupId>
        <artifactId>cloudforge-core</artifactId>
        <version>${project.version}</version>
      </dependency>
      <!-- Central deploy-everything API + CDK synthesis -->
      <dependency>
        <groupId>com.cloudforgeci</groupId>
        <artifactId>cloudforge-api</artifactId>
        <version>${project.version}</version>
      </dependency>
      <!-- Local emulator targets (optional consumer dependencies) -->
      <dependency>
        <groupId>com.cloudforgeci</groupId>
        <artifactId>cloudforge-ministack</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>com.cloudforgeci</groupId>
        <artifactId>cloudforge-localstack</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>com.cloudforgeci</groupId>
        <artifactId>cloudforge-manager-deployment</artifactId>
        <version>${cloudforge-manager-deployment.version}</version>
      </dependency>

      <!-- Netty override, see ${netty.version}'s own comment above. AWS SDK v2's netty-nio-client
           pulls these in transitively at 4.1.107.Final; every artifact it actually resolves needs
           managing here or Maven's nearest-wins rule would leave some of them on the older,
           vulnerable version even with the others overridden. -->
      <dependency>
        <groupId>io.netty</groupId>
        <artifactId>netty-buffer</artifactId>
        <version>${netty.version}</version>
      </dependency>
      <dependency>
        <groupId>io.netty</groupId>
        <artifactId>netty-codec</artifactId>
        <version>${netty.version}</version>
      </dependency>
      <dependency>
        <groupId>io.netty</groupId>
        <artifactId>netty-codec-http</artifactId>
        <version>${netty.version}</version>
      </dependency>
      <dependency>
        <groupId>io.netty</groupId>
        <artifactId>netty-codec-http2</artifactId>
        <version>${netty.version}</version>
      </dependency>
      <dependency>
        <groupId>io.netty</groupId>
        <artifactId>netty-common</artifactId>
        <version>${netty.version}</version>
      </dependency>
      <dependency>
        <groupId>io.netty</groupId>
        <artifactId>netty-handler</artifactId>
        <version>${netty.version}</version>
      </dependency>
      <dependency>
        <groupId>io.netty</groupId>
        <artifactId>netty-resolver</artifactId>
        <version>${netty.version}</version>
      </dependency>
      <dependency>
        <groupId>io.netty</groupId>
        <artifactId>netty-transport</artifactId>
        <version>${netty.version}</version>
      </dependency>
      <dependency>
        <groupId>io.netty</groupId>
        <artifactId>netty-transport-classes-epoll</artifactId>
        <version>${netty.version}</version>
      </dependency>
      <dependency>
        <groupId>io.netty</groupId>
        <artifactId>netty-transport-native-unix-common</artifactId>
        <version>${netty.version}</version>
      </dependency>

      <!-- AWS CDK Dependencies -->
      <dependency>
        <groupId>software.amazon.awscdk</groupId>
        <artifactId>aws-cdk-lib</artifactId>
        <version>${cdk.version}</version>
      </dependency>
      <dependency>
        <groupId>software.constructs</groupId>
        <artifactId>constructs</artifactId>
        <version>${constructs.version}</version>
      </dependency>

      <!-- CDK Nag for compliance validation -->
      <dependency>
        <groupId>io.github.cdklabs</groupId>
        <artifactId>cdknag</artifactId>
        <version>${cdknag.version}</version>
      </dependency>

      <!-- Jackson JSON Processing -->
      <dependency>
        <groupId>com.fasterxml.jackson.core</groupId>
        <artifactId>jackson-annotations</artifactId>
        <version>${jackson-annotations.version}</version>
      </dependency>
      <dependency>
        <groupId>com.fasterxml.jackson.core</groupId>
        <artifactId>jackson-databind</artifactId>
        <version>${jackson.version}</version>
      </dependency>
      <dependency>
        <groupId>com.fasterxml.jackson.core</groupId>
        <artifactId>jackson-core</artifactId>
        <version>${jackson.version}</version>
      </dependency>
      <dependency>
        <groupId>com.fasterxml.jackson.datatype</groupId>
        <artifactId>jackson-datatype-jsr310</artifactId>
        <version>${jackson.version}</version>
      </dependency>
      <dependency>
        <groupId>com.fasterxml.jackson.dataformat</groupId>
        <artifactId>jackson-dataformat-yaml</artifactId>
        <version>${jackson.version}</version>
      </dependency>

      <!-- Jakarta Validation API -->
      <dependency>
        <groupId>jakarta.validation</groupId>
        <artifactId>jakarta.validation-api</artifactId>
        <version>${jakarta.validation.version}</version>
      </dependency>

      <!-- Test Dependencies -->
      <dependency>
        <groupId>org.junit.jupiter</groupId>
        <artifactId>junit-jupiter</artifactId>
        <version>${jupiter.version}</version>
        <scope>test</scope>
      </dependency>
      <dependency>
        <groupId>org.hibernate.validator</groupId>
        <artifactId>hibernate-validator</artifactId>
        <version>${hibernate.validator.version}</version>
        <scope>test</scope>
      </dependency>
      <dependency>
        <groupId>org.glassfish</groupId>
        <artifactId>jakarta.el</artifactId>
        <version>${jakarta.el.version}</version>
        <scope>test</scope>
      </dependency>
      <dependency>
        <groupId>jakarta.el</groupId>
        <artifactId>jakarta.el-api</artifactId>
        <version>5.0.0</version>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <scm>
    <connection>scm:git:https://github.com/CloudForgeCI/cfc-core.git</connection>
    <developerConnection>scm:git:ssh://git@github.com/CloudForgeCI/cfc-core.git</developerConnection>
    <url>https://maven.pkg.github.com/CloudForgeCI/cfc-core</url>
  </scm>

  <modules>
    <!-- Reactor order: core → target adapters → api → api-dependent leaves. -->
    <module>cloudforge-core</module>
    <module>cloudforge-ministack</module>
    <module>cloudforge-localstack</module>
    <module>cloudforge-api</module>
    <!-- cloudforge-synth-service, cloudforge-manager-deployment and cloudforge-manager live in
         their own repositories (CloudForgeCI/cloudforge-synth-service,
         CloudForgeCI/cloudforge-manager-deployment, CloudForgeCI/cloudforge-manager) and are not
         reactor modules; each builds with its own standalone invocation. The manager and
         manager-deployment directories may exist in a local working tree for development, but are
         gitignored.

         cloudforge-manager-deployment and cloudforge-synth-service declare this pom as their
         Maven <parent>, resolved from the repository via an empty <relativePath/> (never from a
         local checkout), so they inherit its dependencyManagement and release profile without
         being reactor members. cloudforge-manager-deployment publishes to Maven Central (see
         .github/workflows/publish-maven-central.yml); cloudforge-synth-service does not, since
         its only Maven consumer is cloudforge-manager's test scope, which installs it from a
         sibling checkout. cloudforge-manager is a standalone pom with no parent, released from
         its own repository.

         cfc-testing does not depend on cloudforge-manager-deployment; cfc-core's CI only tests
         cloudforge-api, cloudforge-core, cloudforge-localstack and cloudforge-ministack.
    -->
    <!-- cfc-testing: independent consumer / cloudforge-sample entrypoint (BOM import, not in reactor) -->
  </modules>

  <profiles>
    <profile>
      <id>dev</id>
      <activation><activeByDefault>true</activeByDefault></activation>
      <properties>
        <build.version.suffix>-SNAPSHOT</build.version.suffix>
      </properties>



    </profile>
    <!-- Opt-in for CI jobs that need the test run; pairs with the
         skipTests=true default above. `mvn clean install -Pci` (or `verify -Pci`) instead of
         scattering -DskipTests=false across individual workflow steps. -->
    <profile>
      <id>ci</id>
      <properties>
        <skipTests>false</skipTests>
      </properties>
    </profile>
    <profile>
      <id>release</id>
      <properties>
        <build.version.suffix></build.version.suffix>
      </properties>
      <build>
        <pluginManagement>
          <plugins>

            <plugin>
              <groupId>org.apache.maven.plugins</groupId>
              <artifactId>maven-javadoc-plugin</artifactId>
              <version>3.7.0</version>
              <configuration>
                <doclint>none</doclint>
                <failOnError>false</failOnError>
              </configuration>
              <executions>
                <execution>
                  <id>attach-javadocs</id>
                  <goals><goal>jar</goal></goals>
                </execution>
              </executions>
            </plugin>

            <plugin>
              <groupId>org.apache.maven.plugins</groupId>
              <artifactId>maven-gpg-plugin</artifactId>
              <!-- The gpg-agent "Too much data for IPC layer" signing error is independent of
                   the plugin version (3.1.0 through 3.2.4 all exhibit it); it is addressed by the
                   runner pin in the release workflow, not here. 3.2.8 is used because it fixes a
                   separate Windows-specific IPC bug present in 3.2.0-3.2.7. -->
              <version>3.2.8</version>
              <executions>
                <execution>
                  <id>sign-artifacts</id>
                  <phase>verify</phase>
                  <goals><goal>sign</goal></goals>
                </execution>
              </executions>
            </plugin>

            <plugin>
              <groupId>org.apache.maven.plugins</groupId>
              <artifactId>maven-deploy-plugin</artifactId>
              <version>3.1.1</version>
            </plugin>
          </plugins>
        </pluginManagement>
        <plugins>
          <!-- Central Portal publisher (replaces old OSSRH/staging) -->
          <plugin>
            <groupId>org.sonatype.central</groupId>
            <artifactId>central-publishing-maven-plugin</artifactId>
            <version>0.9.0</version>
            <extensions>true</extensions>
            <configuration>
              <publishingServerId>central</publishingServerId>
              <autoPublish>true</autoPublish>        <!-- keep -->
              <waitUntil>validated</waitUntil>
              <ignorePublishedComponents>true</ignorePublishedComponents>
              <!-- Optional if you push SNAPSHOTs -->
              <centralSnapshotsUrl>https://central.sonatype.com/repository/maven-snapshots/</centralSnapshotsUrl>
            </configuration>
          </plugin>

          <plugin>
            <groupId>org.codehaus.mojo</groupId>
            <artifactId>flatten-maven-plugin</artifactId>
            <version>1.7.3</version>
            <configuration>
              <updatePomFile>true</updatePomFile> <!-- publish the flattened POM -->
              <flattenMode>bom</flattenMode>      <!-- or resolveCiFriendliesOnly if you prefer -->
              <flattenedPomFilename>${project.build.directory}/${project.artifactId}-${project.version}.pom</flattenedPomFilename>
            </configuration>
            <executions>
              <execution>
                <id>flatten</id>
                <phase>process-resources</phase>  <!-- before package/verify -->
                <goals><goal>flatten</goal></goals>
              </execution>
              <execution>
                <id>flatten.clean</id>
                <phase>clean</phase>
                <goals><goal>clean</goal></goals>
              </execution>
            </executions>


          </plugin>

          <!-- Attach sources -->
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-source-plugin</artifactId>
            <executions>
              <execution>
                <id>attach-sources</id>
                <phase>verify</phase>
                <goals><goal>jar</goal></goals>
              </execution>
            </executions>
          </plugin>

          <!-- Attach javadocs -->
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-javadoc-plugin</artifactId>
            <!--version>3.6.3</version-->
            <executions>
              <execution><id>attach-javadocs</id><goals><goal>jar</goal></goals></execution>
            </executions>
            <configuration><failOnError>false</failOnError></configuration>
          </plugin>

          <!-- GPG signing (Central requires .asc) -->
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-gpg-plugin</artifactId>
            <!--version>3.0.1</version-->
            <executions>
              <execution>
                <id>sign-artifacts</id>
                <phase>verify</phase>
                <goals><goal>sign</goal></goals>
              </execution>
            </executions>
            <configuration>
              <!-- Works with gpg2 and CI. The gpg-agent "Too much data for IPC layer" error seen
                   with GnuPG 2.4.4 on ubuntu-latest is mitigated by the runs-on pin on the signing
                   jobs in the workflow files; Ubuntu's gnupg1 package is a gpg2 compatibility
                   shim and does not avoid it.
                   Use env vars: GPG_KEYNAME, GPG_PASSPHRASE, GPG_HOMEDIR (optional) -->
              <gpgArguments>
                <arg>--pinentry-mode</arg><arg>loopback</arg>
              </gpgArguments>
              <homedir>${env.GPG_HOMEDIR}</homedir>
            </configuration>
          </plugin>

          <!-- Keep legacy deploy plugin from poking at distributionManagement -->
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-deploy-plugin</artifactId>
            <!--version>3.1.2</version-->
            <configuration><skip>true</skip></configuration>
          </plugin>
        </plugins>

      </build>
    </profile>
    <profile>
      <id>security-scan</id>
      <activation>
        <property>
          <name>security-scan</name>
        </property>
      </activation>
      <build>
        <plugins>
          <!-- OWASP Dependency-Check Plugin - only runs when security-scan profile is active -->
          <plugin>
            <groupId>org.owasp</groupId>
            <artifactId>dependency-check-maven</artifactId>
            <executions>
              <execution>
                <goals>
                  <goal>check</goal>
                </goals>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>

  <build>
    <pluginManagement>
      <plugins>
        <!-- Maven Compiler Plugin -->
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>${maven.compiler.plugin.version}</version>
          <configuration>
            <source>${maven.compiler.source}</source>
            <target>${maven.compiler.target}</target>
            <!-- Required for Spring MVC to resolve @PathVariable/@RequestParam by their Java
                 parameter name when no explicit name is given on the annotation — without it,
                 every such binding fails at runtime with "Name for argument of type [...] not
                 specified, and parameter name information not available via reflection" on the
                 first HTTP request. Unit tests that call controller methods directly bypass
                 Spring's argument resolution and do not catch this. -->
            <parameters>true</parameters>
          </configuration>
        </plugin>
        <!-- Maven Source Plugin -->
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-source-plugin</artifactId>
          <version>${maven.source.plugin.version}</version>
        </plugin>
        <!-- Maven Javadoc Plugin -->
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-javadoc-plugin</artifactId>
          <version>${maven.javadoc.plugin.version}</version>
        </plugin>
        <!-- Maven GPG Plugin -->
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-gpg-plugin</artifactId>
          <version>${maven.gpg.plugin.version}</version>
        </plugin>
        <!-- Maven Deploy Plugin -->
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-deploy-plugin</artifactId>
          <version>${maven.deploy.plugin.version}</version>
        </plugin>
        <!-- JaCoCo Code Coverage Plugin -->
        <plugin>
          <groupId>org.jacoco</groupId>
          <artifactId>jacoco-maven-plugin</artifactId>
          <version>${jacoco.plugin.version}</version>
        </plugin>
        <!-- CycloneDX SBOM Plugin -->
        <plugin>
          <groupId>org.cyclonedx</groupId>
          <artifactId>cyclonedx-maven-plugin</artifactId>
          <version>${cyclonedx.plugin.version}</version>
        </plugin>
        <!-- OWASP Dependency-Check Plugin -->
        <plugin>
          <groupId>org.owasp</groupId>
          <artifactId>dependency-check-maven</artifactId>
          <version>${owasp.plugin.version}</version>
        </plugin>
        <!-- Central Publishing Plugin -->
        <plugin>
          <groupId>org.sonatype.central</groupId>
          <artifactId>central-publishing-maven-plugin</artifactId>
          <version>${central.publishing.plugin.version}</version>
        </plugin>
        <!-- Flatten Maven Plugin -->
        <plugin>
          <groupId>org.codehaus.mojo</groupId>
          <artifactId>flatten-maven-plugin</artifactId>
          <version>${flatten.plugin.version}</version>
        </plugin>
      </plugins>
    </pluginManagement>

    <plugins>
      <plugin>
        <groupId>org.jacoco</groupId>
        <artifactId>jacoco-maven-plugin</artifactId>
        <version>0.8.13</version>
        <executions>
          <execution>
            <goals>
              <goal>prepare-agent</goal>
            </goals>
          </execution>
          <execution>
            <id>report</id>
            <phase>prepare-package</phase>
            <goals>
              <goal>report</goal>
            </goals>
          </execution>
          <execution>
            <id>check</id>
            <phase>verify</phase>
            <goals>
              <goal>check</goal>
            </goals>
            <configuration>
              <rules>
                <rule>
                  <element>BUNDLE</element>
                  <limits>
                    <!-- Fail build if instruction coverage is less than 60% -->
                    <limit>
                      <counter>INSTRUCTION</counter>
                      <value>COVEREDRATIO</value>
                      <minimum>0.50</minimum>
                    </limit>
                    <!-- Fail build if branch coverage is less than 60% -->
                    <limit>
                      <counter>BRANCH</counter>
                      <value>COVEREDRATIO</value>
                      <minimum>0.40</minimum>
                    </limit>
                  </limits>
                </rule>
              </rules>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <!-- CycloneDX SBOM Plugin -->
      <plugin>
        <groupId>org.cyclonedx</groupId>
        <artifactId>cyclonedx-maven-plugin</artifactId>
        <version>2.8.2</version>
        <executions>
          <execution>
            <id>generate-sbom</id>
            <phase>package</phase>
            <goals>
              <goal>makeAggregateBom</goal>
            </goals>
          </execution>
        </executions>
        <configuration>
          <projectType>library</projectType>
          <schemaVersion>1.5</schemaVersion>
          <includeBomSerialNumber>true</includeBomSerialNumber>
          <includeCompileScope>true</includeCompileScope>
          <includeProvidedScope>true</includeProvidedScope>
          <includeRuntimeScope>true</includeRuntimeScope>
          <includeTestScope>false</includeTestScope>
          <includeSystemScope>true</includeSystemScope>
          <includeLicenseText>false</includeLicenseText>
          <outputReactorProjects>true</outputReactorProjects>
          <outputFormat>all</outputFormat>
          <outputName>cfc-core-sbom</outputName>
        </configuration>
      </plugin>
      <!-- OWASP Dependency-Check Plugin - configuration only (execution in security-scan profile) -->
      <plugin>
        <groupId>org.owasp</groupId>
        <artifactId>dependency-check-maven</artifactId>
        <version>12.1.0</version>
        <configuration>
          <!-- Fail build if CVSS score is 7 or higher (High/Critical) -->
          <failBuildOnCVSS>7</failBuildOnCVSS>
          <!-- Aggregate results from all modules -->
          <aggregate>true</aggregate>
          <!-- Output formats: HTML, JSON, CSV, XML, SARIF -->
          <formats>
            <format>HTML</format>
            <format>JSON</format>
            <format>XML</format>
            <format>SARIF</format>
          </formats>
          <!-- Suppress false positives -->
          <suppressionFiles>
            <suppressionFile>dependency-check-suppressions.xml</suppressionFile>
          </suppressionFiles>
          <!-- Enable experimental analyzers for better detection -->
          <assemblyAnalyzerEnabled>true</assemblyAnalyzerEnabled>
          <!-- Skip test dependencies -->
          <skipTestScope>true</skipTestScope>
          <!-- Use NVD API 2.0 with proper configuration -->
          <!-- The old JSON data feeds were deprecated and shut down by NIST -->
          <nvdApiKeyEnvironmentVariable>NVD_API_KEY</nvdApiKeyEnvironmentVariable>

          <!-- Enable NVD API usage -->
          <nvdApiServerId>nvd</nvdApiServerId>

          <!-- Rate limiting for API calls (recommended: 2000ms with API key, 6000ms without) -->
          <!-- With an API key, you get 50 requests per 30 seconds -->
          <nvdApiDelay>2000</nvdApiDelay>
          <nvdMaxRetryCount>10</nvdMaxRetryCount>

          <!-- API endpoint (uses v2.0) -->
          <nvdApiEndpoint>https://services.nvd.nist.gov/rest/json/cves/2.0</nvdApiEndpoint>

          <!-- Local cache directory -->
          <dataDirectory>${user.home}/.m2/dependency-check-data</dataDirectory>

          <!-- NVD Database Configuration -->
          <cveValidForHours>168</cveValidForHours>
          <autoUpdate>false</autoUpdate>

          <!-- Filter NVD data to avoid CVSS v4.0 parsing bugs in dependency-check -->
          <!-- Only fetch CVEs from mid-2020 to mid-2024 (pre-CVSS 4.0 era) -->
          <!-- This avoids the "SAFETY" enum parsing error -->
          <nvdApiStartDate>2020-01-01T00:00:00:000 UTC</nvdApiStartDate>
          <nvdApiEndDate>2024-06-01T00:00:00:000 UTC</nvdApiEndDate>

          <!-- Performance optimizations -->
          <centralAnalyzerUseCache>true</centralAnalyzerUseCache>

          <!-- ===== DISABLE ALL ANALYZERS THAT MAKE EXTERNAL API CALLS ===== -->
          <!-- Only use local file-based analysis to avoid 300k+ API requests -->

          <!-- Enable OSS Index (Sonatype) as alternative to NVD -->
          <!-- Uses Maven server credentials from settings.xml with id 'ossindex' -->
          <!-- See: https://jeremylong.github.io/DependencyCheck/dependency-check-maven/configuration.html -->
          <ossindexAnalyzerEnabled>true</ossindexAnalyzerEnabled>
          <ossindexAnalyzerUseCache>true</ossindexAnalyzerUseCache>
          <ossIndexServerId>ossindex</ossIndexServerId>

          <!-- Disable Node.js analyzers - not needed for Java project -->
          <nodeAuditAnalyzerEnabled>false</nodeAuditAnalyzerEnabled>
          <yarnAuditAnalyzerEnabled>false</yarnAuditAnalyzerEnabled>
          <pnpmAuditAnalyzerEnabled>false</pnpmAuditAnalyzerEnabled>
          <retirejmentsAnalyzerEnabled>false</retirejmentsAnalyzerEnabled>
          <nodePackageAnalyzerEnabled>false</nodePackageAnalyzerEnabled>

          <!-- Disable Ruby/Python analyzers - not needed for Java project -->
          <rubygemsAnalyzerEnabled>false</rubygemsAnalyzerEnabled>
          <bundleAuditAnalyzerEnabled>false</bundleAuditAnalyzerEnabled>
          <pyDistributionAnalyzerEnabled>false</pyDistributionAnalyzerEnabled>
          <pyPackageAnalyzerEnabled>false</pyPackageAnalyzerEnabled>
          <pipAnalyzerEnabled>false</pipAnalyzerEnabled>
          <pipfileAnalyzerEnabled>false</pipfileAnalyzerEnabled>
          <poetryAnalyzerEnabled>false</poetryAnalyzerEnabled>

          <!-- Disable Go/PHP/.NET analyzers - not needed for Java project -->
          <golangDepAnalyzerEnabled>false</golangDepAnalyzerEnabled>
          <golangModAnalyzerEnabled>false</golangModAnalyzerEnabled>
          <composerAnalyzerEnabled>false</composerAnalyzerEnabled>
          <nuspecAnalyzerEnabled>false</nuspecAnalyzerEnabled>
          <nugetconfAnalyzerEnabled>false</nugetconfAnalyzerEnabled>

          <!-- Disable Cocoapods/Swift/Carthage - not needed for Java project -->
          <cocoapodsAnalyzerEnabled>false</cocoapodsAnalyzerEnabled>
          <swiftPackageManagerAnalyzerEnabled>false</swiftPackageManagerAnalyzerEnabled>
          <carthageAnalyzerEnabled>false</carthageAnalyzerEnabled>

          <!-- Disable autoconf/cmake/msbuild - not needed for Java/Maven project -->
          <autoconfAnalyzerEnabled>false</autoconfAnalyzerEnabled>
          <cmakeAnalyzerEnabled>false</cmakeAnalyzerEnabled>
          <msbuildProjectAnalyzerEnabled>false</msbuildProjectAnalyzerEnabled>

          <!-- Disable archive analyzers that extract and scan nested JARs -->
          <!-- This dramatically reduces the number of files scanned -->
          <zipExtensions></zipExtensions>

          <!-- Enable only the analyzers needed for Java/Maven -->
          <jarAnalyzerEnabled>true</jarAnalyzerEnabled>
          <centralAnalyzerEnabled>true</centralAnalyzerEnabled>
          <nexusAnalyzerEnabled>false</nexusAnalyzerEnabled>  <!-- Disable: makes API calls -->
          <artifactoryAnalyzerEnabled>false</artifactoryAnalyzerEnabled>  <!-- Disable: makes API calls -->
        </configuration>
      </plugin>
    </plugins>
  </build>

</project>
